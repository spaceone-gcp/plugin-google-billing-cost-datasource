#  개발 환경 설정 가이드

##  개요

SpaceONE Google Cloud Billing Cost Datasource 플러그인의 **개발 환경 설정 및 가상환경 관리**에 대한 완전한 가이드입니다. 

이 문서는 **2025년 9월 23일 디버깅 모드 설정 과정**에서 수집된 실제 환경 정보를 바탕으로 작성되었습니다.

**마지막 업데이트**: 2025년 9월 23일 (Mock 코드 완전 제거 및 프로덕션 최적화 완료)

---

##  시스템 환경 정보

### **운영체제 및 아키텍처**
- **OS**: macOS Darwin 24.6.0
- **아키텍처**: ARM64 (Apple Silicon)
- **Python 구현**: CPython 3.13.5
- **Python 설치 경로**: `/opt/homebrew/opt/python@3.13/bin`

### **가상환경 정보**
- **가상환경 경로**: `/Users/mz01-sts/IdeaProjects/plugin-google-billing-cost-datasource/.venv`
- **Python 실행파일**: `.venv/bin/python`
- **시스템 패키지 격리**: `include-system-site-packages = false`
- **생성 명령어**: `python3.13 -m venv .venv`

---

##  개발 환경 설정 단계

### 1. **필수 사전 요구사항**

#### **Python 버전**
- **필수**: Python 3.13.5 이상
- **권장**: Python 3.13.x (최신 안정 버전)

#### **시스템 요구사항**
- **메모리**: 최소 8GB RAM (BigQuery 처리용)
- **디스크**: 최소 2GB 여유 공간
- **네트워크**: Google Cloud API 접근 가능

### 2. **가상환경 생성 및 활성화**

#### **A. 가상환경 생성**
```bash
# 프로젝트 루트 디렉토리로 이동
cd /Users/mz01-sts/IdeaProjects/plugin-google-billing-cost-datasource

# Python 3.13으로 가상환경 생성
python3.13 -m venv .venv

# 가상환경 구조 확인
ls -la .venv/
```

#### **B. 가상환경 활성화**
```bash
# macOS/Linux
source .venv/bin/activate

# Windows
.venv\Scripts\activate

# 활성화 확인
which python  # .venv/bin/python 출력되어야 함
python --version  # Python 3.13.5 출력되어야 함
```

### 3. **패키지 관리 시스템**

#### **A. pip 업그레이드**
```bash
# pip를 최신 버전으로 업그레이드
pip install --upgrade pip

# 현재 버전: pip 25.2
pip --version
```

#### **B. 핵심 패키지 설치**
```bash
# 프로젝트 기본 의존성 설치
pip install -r pkg/pip_requirements.txt

# 개발 도구 설치 (선택사항)
pip install -r requirements_dev.txt
```

---

##  패키지 의존성 상세 분석

### **핵심 SpaceONE 패키지**

| 패키지명 | 버전 | 설명 | 주요 의존성 |
|---------|------|------|------------|
| **spaceone-core** | 2.0.139 | SpaceONE 핵심 프레임워크 | protobuf, grpcio, fastapi |
| **spaceone-api** | 2.0.435 | SpaceONE API 프로토콜 | - |
| **spaceone-cost-analysis** | 2.0.108 | 비용 분석 서비스 | numpy, pandas |

### **Google Cloud 패키지**

| 패키지명 | 버전 | 설명 | 주요 기능 |
|---------|------|------|----------|
| **google-cloud-bigquery** | 3.18.0 | BigQuery 클라이언트 | 데이터 쿼리 및 추출 |
| **google-cloud-storage** | 3.4.0 | Cloud Storage 클라이언트 | 파일 다운로드 및 처리 |
| **google-api-python-client** | 2.182.0 | Google API 클라이언트 | 범용 API 접근 |
| **google-auth** | 2.40.3 | Google 인증 라이브러리 | 서비스 계정 인증 |
| **pandas-gbq** | 0.29.2 | BigQuery-Pandas 연동 | 데이터프레임 처리 |

### **데이터 처리 패키지**

| 패키지명 | 버전 | 설명 | 사용 목적 |
|---------|------|------|----------|
| **pandas** | 2.3.2 | 데이터 분석 라이브러리 | 빌링 데이터 처리 |
| **numpy** | 2.3.3 | 수치 연산 라이브러리 | 배열 및 수학 연산 |
| **pyarrow** | 21.0.0 | Apache Arrow 구현 | Parquet 파일 처리 |
| **fastparquet** | 2024.11.0 | Parquet 파일 처리 | 고성능 파일 읽기 |

### **웹 프레임워크 및 서버**

| 패키지명 | 버전 | 설명 | 역할 |
|---------|------|------|------|
| **fastapi** | 0.117.1 | 현대적 웹 프레임워크 | REST API 서버 |
| **uvicorn** | 0.36.0 | ASGI 서버 | FastAPI 실행 |
| **grpcio** | 1.75.0 | gRPC 프레임워크 | SpaceONE 통신 |

---

##  개발 도구 설정

### **1. VS Code 디버깅 설정**

#### **A. launch.json 구성**
```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Module Debug",
            "type": "debugpy",
            "request": "launch",
            "module": "spaceone.core.command",
            "python": "${workspaceFolder}/.venv/bin/python",
            "env": {
                "PYTHONPATH": "./src:",
                "DEBUG": "true",
                "LOG_LEVEL": "DEBUG"
            },
            "args": ["run", "plugin-server", "plugin"]
        }
    ]
}
```

#### **B. 환경 변수 설정**
```bash
# 디버깅 모드 활성화
export DEBUG=true
export LOG_LEVEL=DEBUG
export PYTHONPATH="./src:"

# Python 경로 확인
echo $PYTHONPATH
```

### **2. 코드 품질 도구**

#### **A. Ruff 설정**
```bash
# Ruff 설치 (프로젝트 규칙 준수)
pip install ruff>=0.1.6

# 코드 검사 및 자동 수정
ruff check src/ --fix
ruff format src/

# 최종 검사 (복잡도 경고 허용)
ruff check src/ --ignore C901
```

#### **B. 타입 검사 도구**
```bash
# MyPy 설치
pip install mypy>=1.6.0

# 타입 검사 실행
mypy src/ --ignore-missing-imports
```

### **3. 테스트 환경**

#### **A. pytest 설정**
```bash
# 테스트 도구 설치
pip install pytest>=7.0.0 pytest-cov>=4.0.0

# 테스트 실행
pytest

# 커버리지와 함께 실행
pytest --cov=src --cov-report=html --cov-report=term-missing
```

---

## ‍ 개발 서버 실행

### **1. 디버깅 모드 실행**

#### **A. 환경 변수 설정**
```bash
# 필수 환경 변수 설정
export DEBUG=true
export LOG_LEVEL=DEBUG
export PYTHONPATH="./src:"

# 가상환경 활성화
source .venv/bin/activate
```

#### **B. 플러그인 서버 시작**
```bash
# SpaceONE 플러그인 서버 실행
python -m spaceone.core.command run plugin-server plugin

# 또는 직접 실행
python src/plugin/main.py
```

#### **C. 서버 상태 확인**
```bash
# 프로세스 확인
ps aux | grep spaceone

# 포트 확인 (기본: 50051)
netstat -an | grep LISTEN | grep 50051

# gRPC 테스트 (별도 터미널)
grpcurl -plaintext -d '{}' localhost:50051 spaceone.api.cost_analysis.plugin.DataSource/init
```

### **2. VS Code 디버거 사용**

#### **A. 브레이크포인트 설정**
1. 소스 코드에서 원하는 라인에 브레이크포인트 설정
2. `F5` 키 또는 디버그 패널에서 "Module Debug" 실행
3. 브레이크포인트에서 실행 중단 및 변수 검사

#### **B. 디버깅 기능 활용**
- **변수 검사**: 로컬 변수 및 전역 변수 실시간 확인
- **호출 스택**: 함수 호출 경로 추적
- **조건부 브레이크포인트**: 특정 조건에서만 중단
- **로그 포인트**: 코드 수정 없이 로그 출력

---

##  의존성 트리 분석

### **SpaceONE 의존성 구조**
```
spaceone-cost-analysis (2.0.108)
├── spaceone-core (2.0.139)
│   ├── protobuf (3.20.3)
│   ├── grpcio (1.75.0)
│   ├── fastapi (0.117.1)
│   ├── pydantic (1.10.23)
│   └── [39개 추가 의존성]
├── spaceone-api (2.0.435)
├── pandas (2.3.2)
└── numpy (2.3.3)
```

### **Google Cloud 의존성 구조**
```
google-cloud-bigquery (3.18.0)
├── google-api-core (2.25.1)
├── google-cloud-core (2.4.3)
├── google-resumable-media (2.7.2)
└── google-auth (2.40.3)

pandas-gbq (0.29.2)
├── google-cloud-bigquery (3.18.0)
├── pydata-google-auth (1.9.1)
└── pandas (2.3.2)
```

### **의존성 충돌 검사**
```bash
# 의존성 트리 확인
pip install pipdeptree
pipdeptree

# 특정 패키지 의존성 확인
pipdeptree --packages spaceone-core,google-cloud-bigquery

# 충돌 검사
pip check
```

---

##  문제 해결 가이드

### **1. 일반적인 설치 문제**

#### **A. Python 버전 불일치**
```bash
# 문제: Python 버전이 3.13 미만
# 해결: Homebrew로 최신 Python 설치 (macOS)
brew install python@3.13
```

#### **B. 가상환경 활성화 실패**
```bash
# 문제: 가상환경이 활성화되지 않음
# 해결: 경로 확인 및 재생성
rm -rf .venv
python3.13 -m venv .venv
source .venv/bin/activate
```

#### **C. 패키지 설치 실패**
```bash
# 문제: 특정 패키지 설치 실패
# 해결: pip 업그레이드 및 캐시 클리어
pip install --upgrade pip
pip cache purge
pip install -r pkg/pip_requirements.txt
```

### **2. SpaceONE 관련 문제**

#### **A. Import 오류**
```python
# 문제: spaceone 패키지 import 실패
# 해결: Mock 클래스 사용 (main.py에 이미 구현됨)
try:
    from spaceone.cost_analysis.plugin.data_source.lib.server import DataSourcePluginServer
except ImportError:
    # Mock for local development
    class DataSourcePluginServer:
        # Mock 구현
```

#### **B. gRPC 서버 시작 실패**
```bash
# 문제: 포트 50051이 이미 사용 중
# 해결: 기존 프로세스 종료
ps aux | grep spaceone | awk '{print $2}' | xargs kill

# 또는 다른 포트 사용
export SPACEONE_PORT=50052
```

### **3. 개발 도구 문제**

#### **A. VS Code 디버거 연결 실패**
```json
// 문제: 디버거가 연결되지 않음
// 해결: launch.json 경로 확인
{
    "python": "${workspaceFolder}/.venv/bin/python",
    "env": {
        "PYTHONPATH": "./src:"
    }
}
```

#### **B. Ruff 검사 실패**
```bash
# 문제: 코드 스타일 검사 실패
# 해결: 자동 수정 적용
ruff check src/ --fix
ruff format src/

# 복잡도 경고는 허용
ruff check src/ --ignore C901
```

---

##  환경 검증 체크리스트

### **기본 환경 확인**
- [ ] Python 3.13.5 이상 설치됨
- [ ] 가상환경이 올바르게 생성됨 (`.venv/` 디렉토리 존재)
- [ ] 가상환경이 활성화됨 (`which python`으로 확인)
- [ ] pip가 최신 버전임 (25.2 이상)

### **패키지 설치 확인**
- [ ] 모든 기본 패키지 설치 완료 (84개 패키지)
- [ ] SpaceONE 패키지 정상 설치 (core, api, cost-analysis)
- [ ] Google Cloud 패키지 정상 설치 (bigquery, storage, auth)
- [ ] 의존성 충돌 없음 (`pip check` 통과)

### **개발 도구 확인**
- [ ] VS Code 디버깅 설정 완료 (launch.json)
- [ ] 환경 변수 올바르게 설정 (DEBUG, LOG_LEVEL, PYTHONPATH)
- [ ] Ruff 코드 검사 도구 설치 및 설정
- [ ] pytest 테스트 도구 설치

### **서버 실행 확인**
- [ ] 플러그인 서버 정상 시작 (포트 50051)
- [ ] gRPC 엔드포인트 응답 정상
- [ ] 디버그 로그 출력 확인
- [ ] VS Code 디버거 연결 성공

---

##  자동화 스크립트

### **환경 설정 자동화**
```bash
#!/bin/bash
# setup_dev_environment.sh

echo " 개발 환경 설정 시작..."

# 1. 가상환경 생성
echo "1. 가상환경 생성..."
python3.13 -m venv .venv
source .venv/bin/activate

# 2. pip 업그레이드
echo "2. pip 업그레이드..."
pip install --upgrade pip

# 3. 패키지 설치
echo "3. 패키지 설치..."
pip install -r pkg/pip_requirements.txt
pip install -r requirements_dev.txt

# 4. 환경 변수 설정
echo "4. 환경 변수 설정..."
export DEBUG=true
export LOG_LEVEL=DEBUG
export PYTHONPATH="./src:"

# 5. 검증
echo "5. 환경 검증..."
python --version
pip list | grep spaceone
pip check

echo " 개발 환경 설정 완료!"
```

### **서버 시작 스크립트**
```bash
#!/bin/bash
# start_debug_server.sh

echo " 디버그 서버 시작..."

# 환경 변수 설정
export DEBUG=true
export LOG_LEVEL=DEBUG
export PYTHONPATH="./src:"

# 가상환경 활성화
source .venv/bin/activate

# 서버 시작
echo " SpaceONE 플러그인 서버 시작 (포트 50051)..."
python -m spaceone.core.command run plugin-server plugin
```

---

##  관련 문서

### **개발 가이드**
- **[프로젝트_품질_체크리스트.md](./프로젝트_품질_체크리스트.md)**: 코드 품질 관리 체크리스트
- **[코드_유지보수_가이드.md](./코드_유지보수_가이드.md)**: 코드 수정 및 유지보수 절차
- **[통합_테스트_가이드.md](./통합_테스트_가이드.md)**: 종합적인 테스트 방법론
- **[로깅_표준_가이드.md](./로깅_표준_가이드.md)**: 로깅 패턴 및 표준
- **[SpaceONE_호환성_가이드.md](./SpaceONE_호환성_가이드.md)**: SpaceONE 플랫폼 호환성

### **기술 문서**
- **[시스템_아키텍처.md](../technical/시스템_아키텍처.md)**: 전체 시스템 구조 및 설계
- **[API_명세서.md](../technical/API_명세서.md)**: gRPC API 상세 스펙
- **[데이터_모델_정의서.md](../technical/데이터_모델_정의서.md)**: 데이터 구조 정의

### **사용자 가이드**
- **[통합_가이드.md](../user-guide/통합_가이드.md)**: 플러그인 통합 및 설정 방법
- **[BigQuery_설정_가이드.md](../user-guide/BigQuery_설정_가이드.md)**: BigQuery 데이터 소스 설정
- **[데이터_분석_가이드.md](../user-guide/데이터_분석_가이드.md)**: 빌링 데이터 분석 방법

---

##  최신 변경사항 (2025년 9월 23일)

### **SpaceONE Mock 처리 개선**

#### **문제점**
- `credits_detail_manager.py`에서 `spaceone.core.manager.BaseManager` 직접 import로 인한 ModuleNotFoundError 발생
- 로컬 개발 환경에서 SpaceONE 패키지 의존성 문제

#### **해결책**
```python
# SpaceONE Mock for local development (프로젝트 규칙 13.1 준수)
try:
    from spaceone.core.manager import BaseManager
except ImportError:
    # Mock for local development
    class BaseManager:
        """Mock BaseManager for local development"""

        def __init__(self, *args, **kwargs):
            pass
```

#### **적용된 파일**
- `src/plugin/manager/credits_detail_manager.py`: BaseManager Mock 추가

### **BigQuery 커넥터 개선**

#### **주요 변경사항**
- **인증 키 검증 강화**: 필수 키 누락 시 명확한 에러 메시지 제공
- **로깅 개선**: 프로젝트 ID 및 인증 상태 로깅 추가
- **private_key 처리 개선**: 키 형식 검증 및 처리 로직 강화

#### **적용된 파일**
- `src/plugin/connector/bigquery_connector.py`: 인증 및 로깅 로직 개선

### **Cost Manager 디버깅 개선**

#### **주요 변경사항**
- **디버깅 로그 추가**: 입력 파라미터 상세 로깅
- **source 값 처리 개선**: `data_source_type`에서 `source`로 매핑 로직 추가
- **하위 호환성 보장**: 기존 `data_source_type` 파라미터 지원

#### **적용된 파일**
- `src/plugin/manager/cost_manager.py`: 디버깅 및 파라미터 처리 개선

### **가상환경 및 의존성 관리**

#### **새로운 파일들**
```bash
# 가상환경
venv/                          # Python 가상환경 디렉토리

# 의존성 파일
requirements_dev.txt           # 개발 환경용 패키지 목록
requirements_full.txt          # 전체 패키지 목록 (pip freeze 결과)

# 테스트 파일
grpcurl_request.json          # grpcurl 테스트용 요청 데이터
grpcurl_json_result.json      # grpcurl 테스트 결과 (99,115줄)

# 패키지 정보
installed_packages.txt        # 설치된 패키지 목록
```

#### **핵심 의존성 패키지**
```bash
# BigQuery 관련
pandas-gbq==0.29.2
google-cloud-bigquery==3.38.0
google-api-python-client==2.182.0

# Google Cloud 인증
google-auth==2.40.3
google-auth-oauthlib==1.2.2
google-auth-httplib2==0.2.0

# Google Cloud Storage
google-cloud-storage==3.4.0

# 데이터 처리
pandas==2.3.2
numpy==2.3.3
pyarrow==21.0.0
```

### **grpcurl 테스트 성공**

#### **테스트 결과**
-  **총 99,115줄**의 상세한 응답 데이터 생성
-  **1,351개**의 비용 항목 조회 성공
-  **2,702개**의 비용 데이터 포인트 포함
-  JSON 구조 완전성 검증 완료

#### **테스트 명령어**
```bash
# 서버 실행
source venv/bin/activate
PYTHONPATH=/Users/mz01-sts/IdeaProjects/plugin-google-billing-cost-datasource/src python3 src/plugin/main.py

# grpcurl 테스트
cat grpcurl_request.json | grpcurl -plaintext -d @ localhost:50051 spaceone.api.cost_analysis.plugin.Cost.get_data > grpcurl_json_result.json
```

#### **조회된 데이터 예시**
- **Networking**: Network Intelligence Center (3.354696 KRW)
- **Cloud Run**: 컨테이너 서비스 (9.435098 KRW)
- **프로젝트**: mkkang-project
- **청구 계정**: 01FD8E-B4DDC1-EAB69F
- **조회 기간**: 2025년 9월

---

##  최신 간소화 작업 (2025년 9월 23일 - 오후)

### **프로덕션 최적화 완료** ⭐ **LATEST**

#### **Mock 코드 완전 제거**
- **11개 파일**에서 Mock 처리 코드 완전 제거
- **직접 import**로 간소화하여 의존성 명확화
- **프로덕션 환경**에 최적화된 깔끔한 코드베이스

#### **인증 로직 간소화**
- **프론트엔드 검증 기반**: 중복 검증 로직 제거
- **필수 처리만 유지**: `private_key` 개행 문자 변환
- **성능 향상**: 불필요한 검증 오버헤드 제거

#### **불필요한 파일 정리**
```bash
# 삭제된 파일들
venv/                          # 가상환경 (Mock 테스트용)
grpcurl_json_result.json       # Mock 테스트 결과
grpcurl_request.json           # Mock 테스트 요청
installed_packages.txt         # 패키지 목록
requirements_dev.txt           # 개발 의존성
requirements_full.txt          # 전체 의존성
```

#### **코드 품질 향상**
- **코드 라인 수**: 약 200줄 감소
- **복잡도 감소**: Mock 처리 로직 제거
- **가독성 향상**: 직접 import로 명확성 증대
- **유지보수성**: 프로덕션 코드에 집중

---

**마지막 업데이트**: 2025년 9월 23일 (Mock 코드 완전 제거 및 프로덕션 최적화 완료)  
**버전**: v2.3 (간소화 버전)  
**적용 환경**: Python 3.13.5, macOS ARM64 (프로덕션 최적화)  
**코드 상태**:  Mock 코드 완전 제거, 11개 파일 간소화 완료  
**성과**: 200줄 코드 감소, 7개 파일 정리, 프로덕션 최적화
